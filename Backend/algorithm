1 - Start  
2 - Include necessary libraries:  
   2.1 - Include <stdio.h>  
   2.2 - Include <string.h>  
3 - Define the `generateAssembly` function to generate assembly code from the given Three Address Code (TAC):  
   3.1 - Accept `result`, `arg1`, `operator`, and `arg2` as arguments  
   3.2 - Use a `switch` statement to handle different operators:  
      3.2.1 - For addition ('+'):  
         3.2.1.1 - Print assembly code for loading `arg1` into `AX`  
         3.2.1.2 - Print assembly code for adding `arg2` to `AX`  
         3.2.1.3 - Print assembly code for storing the result in `result`  
      3.2.2 - For subtraction ('-'):  
         3.2.2.1 - Similar to addition but use the `SUB` instruction  
      3.2.3 - For multiplication ('*'):  
         3.2.3.1 - Similar to addition but use the `MUL` instruction  
      3.2.4 - For division ('/'):  
         3.2.4.1 - Similar to addition but use the `DIV` instruction  
      3.2.5 - For assignment ('='):  
         3.2.5.1 - Print assembly code for moving `arg1` into `AX` and storing it in `result`  
      3.2.6 - For `goto` ('>'):  
         3.2.6.1 - Print assembly code for a jump to the label `result`  
      3.2.7 - For any other operator, print "Invalid operator."  
4 - Define the `parseAndGenerate` function to parse TAC and generate corresponding assembly:  
   4.1 - Use `sscanf` to match the format `result = arg1 operator arg2` and call `generateAssembly`  
   4.2 - Use `sscanf` to match the format `result = arg1` and call `generateAssembly`  
   4.3 - Use `sscanf` to match a `goto` statement and call `generateAssembly` with the appropriate arguments  
   4.4 - If the TAC does not match any expected format, print "Invalid Three Address Code."  
5 - Define the `main` function:  
   5.1 - Declare a string `tac[10]` to store the Three Address Code input  
   5.2 - Display the message "Enter the three address code (to exit, type 'exit')"  
   5.3 - Use a `while` loop to repeatedly prompt the user for input:  
      5.3.1 - Display a prompt "> "  
      5.3.2 - Read the input TAC using `fgets`  
      5.3.3 - Remove the newline character at the end of the input  
      5.3.4 - If the input is "exit", break the loop to exit the program  
      5.3.5 - Call `parseAndGenerate` to process the TAC and generate assembly  
6 - End

1 - Start  
2 - Declare a constant SIZE = 1024  
3 - Define functions isOperator, isDelimiter, isLiteral, and isKeyword  
   3.1 - isOperator(ch)  
      3.1.1 - Check whether ch is in "+-*/="  
      3.1.2 - If true, print operator message and return 1  
      3.1.3 - Otherwise, return 0  

   3.2 - isDelimiter(ch)  
      3.2.1 - Check whether ch is in "(){},;"  
      3.2.2 - If true, print delimiter message and return 1  
      3.2.3 - Otherwise, return 0  

   3.3 - isLiteral(token)  
      3.3.1 - Repeat for i = 0 to strlen(token) - 1  
         3.3.1.1 - Check whether token[i] is not a digit (0-9)  
         3.3.1.2 - If true, return 0  
      3.3.2 - Print literal message and return 1  

   3.4 - isKeyword(token)  
      3.4.1 - Check whether token is "int", "float", or "char"  
      3.4.2 - If true, print keyword message and return 1  
      3.4.3 - Otherwise, return 0  

4 - Define main() function  
5 - Declare variables  
   5.1 - char line[SIZE], token[SIZE]  
   5.2 - FILE *file  

6 - Initialize token as an empty string  
7 - Open "input.txt" in read mode and store the file pointer in file  
8 - Repeat while reading each line from the file  
   8.1 - Remove the newline character from line  
   8.2 - Check whether line is a single-line comment (// at the beginning)  
      8.2.1 - If true, continue to the next line  
   8.3 - Check whether line starts with /* (multi-line comment)  
      8.3.1 - If true, repeat reading the next lines  
         8.3.1.1 - Check whether */ appears (ending multi-line comment)  
         8.3.1.2 - If true, break the loop  
      8.3.2 - Continue to the next line  
   8.4 - Print the line  
   8.5 - Repeat for i = 0 to strlen(line) - 1  
      8.5.1 - Check whether line[i] is an operator, delimiter, or space  
         8.5.1.1 - Check whether token is not empty  
            8.5.1.1.1 - Check whether token is a keyword  
            8.5.1.1.2 - If false, check whether it is a literal  
            8.5.1.1.3 - If false, classify it as an identifier and print  
            8.5.1.1.4 - Reset token to an empty string  
      8.5.2 - Otherwise, if line[i] is not a space  
         8.5.2.1 - Append line[i] to token  
   8.6 - Print a new line for formatting  

9 - End
